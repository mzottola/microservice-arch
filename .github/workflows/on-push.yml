name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "github sha is $GITHUB_SHA"
      - run: echo "github.sha is ${{ github.sha }}"
